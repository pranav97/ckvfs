# ckvfs
This repo is for the C implementation of the python kvfs - https://github.com/qznc/kvfs

# Running instructions
1. Build - ```$ make```
2. ```$ ./main -d -s -f /tmp/hellofs2```

Dependencies:
1. OPENSSL - to install run ```apt-get install libssl-dev```